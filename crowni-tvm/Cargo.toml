[package]
name = "crowni-tvm"
version = "0.2.0"
edition = "2021"
authors = ["KPS (Han Seon)"]
description = "CROWNIN Balanced Ternary Meta-Kernel — TVM + Scheduler + Permission + Transaction + CTP + FPGA Bridge"

[dependencies]
